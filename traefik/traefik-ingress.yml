apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  entryPoints:
    - http
  routes:
    - match: Host(`test.thexiao77.com`)
      kind: Rule
      services:
        - name: traefik
          port: 30080
